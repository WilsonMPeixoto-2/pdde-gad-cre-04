// The approved cover is rendered inside SectionIntro so it shares the
// document shell, sidebar and responsive grid. Keeping this export avoids
// changing the public component contract while removing the obsolete
// full-screen promotional hero that preceded the guide navigation.
export const HeroCover = () => null;
